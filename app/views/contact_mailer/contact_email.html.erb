<!DOCTYPE html>
<html>
  <head>
  </head>
  
  <body>
    <div class="container">
      <p>You have received a message from the site's contact form, from
      <%= "#{@name}, #{@email}." %></p>
      <p><%= @body %></p>
    </div>
  </body>
</html>